{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        {% for post in posts %}

            <div class="single-post">

                <h3>{{ post.title }}</h3>
                <p>{{ post.content }}</p>

            </div>


        {% endfor %}
    </div>

{% endblock %}
